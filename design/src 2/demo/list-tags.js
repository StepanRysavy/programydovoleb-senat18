/* eslint-disable */

const DEMO_TAGS_RESPONSE = {
  data: {
    list: [
      {
        id: 0,
        en: 'demo',
        cs: 'demo'
      }
    ]
  }
};

export default JSON.stringify(DEMO_TAGS_RESPONSE);

/* eslint-enable */
